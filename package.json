{
  "name": "akira-library",
  "version": "0.1.0",
  "private": true,
  "description": "A Zotero-style YAML reference manager built with Tauri.",
  "scripts": {
    "dev": "tauri dev",
    "dev:web": "mkdir -p vendor && cp node_modules/js-yaml/dist/js-yaml.min.js vendor/js-yaml.min.js && python3 -m http.server 5173",
    "build": "tauri build",
    "build:web": "node scripts/build-web.mjs",
    "preview:web": "npm run build:web && python3 -m http.server 5173 -d dist"
  },
  "dependencies": {
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4"
  }
}
